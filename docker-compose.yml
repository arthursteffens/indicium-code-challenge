version: '3'

services:
  pg_db:
    image: postgres:12
    environment:
      POSTGRES_DB: northwind
      POSTGRES_USER: northwind_user
      POSTGRES_PASSWORD: thewindisblowing
    volumes:
      - ./dbdata:/var/lib/postgresql/data
      - ./data/northwind.sql:/docker-entrypoint-initdb.d/northwind.sql
    ports:
      - 5432:5432
    networks:
      - arthur-compose-network

  mysql_db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: mysql_northwind
      MYSQL_USER: user
      MYSQL_PASSWORD: userpass
      MYSQL_ROOT_PASSWORD: rootpass
    command: mysqld --sql_mode="" --init-connect='SET NAMES UTF8;' --innodb-flush-log-at-trx-commit=0
    ports:
      - 3306:3306
    volumes:
      - ./mysqldata/data:/var/lib/mysql
      - ./data/mysql.sql:/docker-entrypoint-initdb.d/mysql.sql
    networks:
      - arthur-compose-network
    
networks: 
  arthur-compose-network:
    driver: bridge